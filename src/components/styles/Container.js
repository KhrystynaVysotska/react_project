import React from "react";
import styled from "styled-components";
import { fontSize } from "../constants/Constants.js";

const Container = styled.div`
  padding: ${fontSize.fs36} ${fontSize.fs208};
`;

export default Container;
