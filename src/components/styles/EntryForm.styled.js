import styled from "styled-components";
import { colors } from "../constants/Constants";

const EntryFormStyled = styled.div`
  width: 100vw;
  height: 100vh;
  background-color: ${colors.lightprimary};
  margin: 0;
  padding: 0;
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default EntryFormStyled;
