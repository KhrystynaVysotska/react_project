import styled from "styled-components";
import { fontSize } from "../constants/Constants.js";

const LoadMoreButtonStyled = styled.div`
  margin-top: ${fontSize.fs24};
  width: 100%;
  text-align: center;
`;

export default LoadMoreButtonStyled;
