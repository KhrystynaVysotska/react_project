import styled from "styled-components";
import { fontSize } from "../constants/Constants.js";

const AddToCartButtonStyled = styled.div`
  width: 100%;
  align-items: center;
  justify-content: center;
  display: flex;
  margin-top: ${fontSize.fs36};
`;

export default AddToCartButtonStyled;
