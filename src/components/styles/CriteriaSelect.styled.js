import styled from "styled-components";
import { fontSize } from "../constants/Constants";
const CriteriaSelectStyled = styled.div`
  width: 100%;
  margin-bottom: ${fontSize.fs36};
  margin-top: ${fontSize.fs36};
  display: flex;
  justify-content: space-between;
`;

export default CriteriaSelectStyled;
